/**
 * Application related global configurations.
 */

import { lightTheme, sidebarConfig } from '@appConfig';

export default {
  appName: 'Optym5',
  appTheme: lightTheme,
  sidebar: sidebarConfig,
};
